# Shinzo AI — Development Roadmap

See `README.md` §6 for the live status table (kept in sync with this file's milestone list).
This file exists to satisfy Milestone 0's expected documentation set; the canonical, continuously
updated tracker is the project root `README.md` — always check there first for current status.

Milestone order: Architecture docs -> FastAPI foundation -> Basic chat -> Personality spec ->
Dataset pipeline -> Custom dataset -> Fine-tuning -> Evaluation -> Memory -> Emotion -> Social
Intelligence -> Human Essence -> RAG -> Proactive Engine -> Notifications -> Full Orchestrator ->
Public API -> API Security -> Website Integration -> Full Testing -> Deployment.

Acceptance criteria per milestone are defined at the point each milestone is implemented (see the
corresponding module's tests in `tests/`).
